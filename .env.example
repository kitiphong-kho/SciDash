SCOPUS_API_KEY=put_your_scopus_api_key_here
# Optional: only needed if Elsevier issued you an InstToken (e.g. for
# off-campus/API-key-only access that returns restricted results).
SCOPUS_INST_TOKEN=
